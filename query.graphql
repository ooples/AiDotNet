{ 
  repository(owner: "ooples", name: "AiDotNet") { 
    pullRequest(number: 304) { 
      reviewThreads(last: 100) { 
        nodes { 
          id 
          isResolved
          comments(first: 1) { 
            nodes {
              body
              path
            }
          }
        }
      }
    }
  }
}
