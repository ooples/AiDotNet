---
const isDev = import.meta.env.PUBLIC_ENV_MODE === 'dev';
---

{isDev && (
  <div class="fixed top-0 left-0 right-0 z-[100] bg-amber-500 text-black text-center text-xs font-bold py-1 tracking-wider">
    DEV / TEST ENVIRONMENT — Not for public use
  </div>
)}
