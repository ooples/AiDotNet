---
title: "ValidationOptions"
description: "Options for controlling prompt validation behavior."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.PromptEngineering.Analysis`

Options for controlling prompt validation behavior.

## For Beginners

Settings for how strict the validation should be.

Example:

## How It Works

Configures what checks are performed and how strict validation should be.

## Properties

| Property | Summary |
|:-----|:--------|
| `CheckForInjection` | Gets or sets whether to check for potential prompt injection. |
| `Lenient` | Gets a lenient validation configuration. |
| `MaxTokens` | Gets or sets the maximum allowed token count. |
| `MinSeverityToReport` | Gets or sets the minimum severity level to report. |
| `Strict` | Gets a strict validation configuration. |
| `ValidateVariables` | Gets or sets whether to validate template variables. |

