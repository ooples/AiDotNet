---
title: "GriffinLim<T>"
description: "Griffin-Lim algorithm for audio reconstruction from magnitude spectrograms."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Diffusion.Audio`

Griffin-Lim algorithm for audio reconstruction from magnitude spectrograms.

## For Beginners

When we compute a spectrogram, we get both magnitude
(how loud each frequency is) and phase (where in its cycle each frequency is).
For visualization and some ML tasks, we often discard phase and keep only magnitude.

But to play back audio, we need phase information! Griffin-Lim solves this by:

1. Starting with random phase
2. Converting to audio (ISTFT)
3. Converting back to spectrogram (STFT)
4. Keeping the new phase but forcing the original magnitude
5. Repeating until convergence

With momentum acceleration, convergence is typically achieved in 30-60 iterations.

Usage:
```cs
var griffinLim = new GriffinLim<float>(
nFft: 2048,
hopLength: 512,
iterations: 60
);

// From a magnitude spectrogram (e.g., generated by AI)
var audio = griffinLim.Reconstruct(magnitudeSpectrogram);
```

## How It Works

The Griffin-Lim algorithm iteratively estimates the phase of a signal given
only its magnitude spectrogram. This is useful when you have a spectrogram
(e.g., from a generative model like Riffusion) but need to reconstruct audio.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `GriffinLim(Int32,Nullable<Int32>,IWindowFunction<>,Int32,Double,Nullable<Int32>)` | Initializes a new Griffin-Lim processor. |
| `GriffinLim(ShortTimeFourierTransform<>,Int32,Double,Nullable<Int32>)` | Initializes Griffin-Lim with an existing STFT processor. |

## Properties

| Property | Summary |
|:-----|:--------|
| `Engine` | Gets the engine for GPU-accelerated operations. |
| `Iterations` | Gets the number of iterations. |
| `Momentum` | Gets the momentum factor. |
| `STFT` | Gets the STFT processor. |

## Methods

| Method | Summary |
|:-----|:--------|
| `ApplyMomentum(Tensor<>,Tensor<>,Tensor<>)` | Applies momentum to phase update for faster convergence. |
| `ClonePhase(Tensor<>)` | Clones a phase tensor. |
| `ComputeSpectralConvergence(Tensor<>,Tensor<>)` | Estimates the spectral convergence error. |
| `CopyPhase(Tensor<>,Tensor<>)` | Copies phase from source to destination. |
| `ExtractPhase(Tensor<Complex<>>)` | Extracts phase from complex spectrogram. |
| `InitializeRandomPhase(Int32,Int32)` | Initializes random phase in range [-pi, pi]. |
| `PolarToComplex(Tensor<>,Tensor<>)` | Creates complex spectrogram from magnitude and phase. |
| `Reconstruct(Tensor<>,Nullable<Int32>)` | Reconstructs audio from a magnitude spectrogram using Griffin-Lim. |
| `ReconstructFromMel(Tensor<>,MelSpectrogram<>,Nullable<Int32>)` | Reconstructs audio from a Mel spectrogram. |
| `ReconstructWithProgress(Tensor<>,Action<Int32,Double>,Nullable<Int32>)` | Reconstructs audio with progress callback. |

## Fields

| Field | Summary |
|:-----|:--------|
| `NumOps` | Provides numeric operations for the specific type T. |
| `_hopLength` | Hop length for GPU operations. |
| `_iterations` | Number of iterations for phase estimation. |
| `_momentum` | Momentum factor for accelerated convergence. |
| `_nFft` | FFT size for GPU operations. |
| `_random` | Random number generator for initial phase. |
| `_stft` | The STFT processor. |
| `_windowTensor` | Window tensor for GPU operations. |

