---
title: "SDXLRefiner<T>"
description: "SDXL Refiner model for enhancing generated images."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Diffusion.TextToImage`

SDXL Refiner model for enhancing generated images.

## For Beginners

Think of the refiner as a "finishing touch" step:

Without refiner:
Base image (good structure, okay details)

With refiner:
Refined image (same structure, enhanced details like texture, sharpness)

The refiner uses lower noise levels (typically 0.2-0.4 denoising strength)
to preserve the overall composition while enhancing fine details.

## How It Works

The SDXL refiner is a specialized model trained to enhance the fine details
of images generated by the SDXL base model. It operates in the latent space
and is designed to work with partially denoised latents.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `SDXLRefiner(UNetNoisePredictor<>,IVAEModel<>,INoiseScheduler<>,IConditioningModule<>,Nullable<Int32>)` | Initializes a new SDXL refiner. |

## Methods

| Method | Summary |
|:-----|:--------|
| `AddNoiseAtTimestep(Tensor<>,Tensor<>,Int32)` | Adds noise at a specific timestep. |
| `ApplyGuidance(Tensor<>,Tensor<>,Double)` | Applies classifier-free guidance. |
| `Refine(Tensor<>,String,String,Int32,Double,Nullable<Int32>)` | Refines an image by enhancing details. |
| `SampleNoise(Int32[],Random)` | Samples noise tensor. |

## Fields

| Field | Summary |
|:-----|:--------|
| `NumOps` | Provides numeric operations for the specific type T. |
| `_conditioner` | The conditioning module. |
| `_random` | Random number generator. |
| `_refinerUNet` | The refiner U-Net. |
| `_scheduler` | The scheduler. |
| `_vae` | The VAE (shared with base model). |

