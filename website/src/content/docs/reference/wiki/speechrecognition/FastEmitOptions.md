---
title: "FastEmitOptions"
description: "Options for FastEmit: low-latency RNN-Transducer with emission regularization."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.SpeechRecognition.Streaming`

Options for FastEmit: low-latency RNN-Transducer with emission regularization.

## For Beginners

These options configure the FastEmit model. Default values follow the original paper's recommended settings for optimal speech recognition accuracy.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `FastEmitOptions` | Initializes a new instance with default values. |
| `FastEmitOptions(FastEmitOptions)` | Initializes a new instance by copying from another instance. |

