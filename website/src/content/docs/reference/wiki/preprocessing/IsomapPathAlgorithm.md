---
title: "IsomapPathAlgorithm"
description: "Specifies the shortest path algorithm for Isomap."
section: "API Reference"
---

`Enums` · `AiDotNet.Preprocessing.DimensionalityReduction`

Specifies the shortest path algorithm for Isomap.

## Fields

| Field | Summary |
|:-----|:--------|
| `Dijkstra` | Dijkstra's algorithm (O(n² log n) for all pairs). |
| `FloydWarshall` | Floyd-Warshall algorithm (O(n³), finds all pairs). |

