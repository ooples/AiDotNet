---
title: "LatentDirichletAllocation<T>"
description: "Latent Dirichlet Allocation for topic modeling."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Preprocessing.DimensionalityReduction`

Latent Dirichlet Allocation for topic modeling.

## For Beginners

LDA finds hidden themes (topics) in your data:

- Input: Document-term matrix (rows=documents, columns=word counts)
- Output: Document-topic distribution (what topics each document is about)
- Also learns: Topic-word distribution (what words define each topic)
- Use for: Topic modeling, document clustering, feature extraction

## How It Works

LDA is a generative probabilistic model that discovers latent topics in a collection
of documents (or more generally, in count data). Each document is modeled as a mixture
of topics, and each topic is a distribution over words (features).

The model assumes:

- Each document has a distribution over topics (theta)
- Each topic has a distribution over words (beta)
- Words in documents are generated by first sampling a topic, then sampling a word

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `LatentDirichletAllocation(Int32,Nullable<Double>,Nullable<Double>,LdaLearningMethod,Int32,Double,Int32,Nullable<Int32>,Int32[])` | Creates a new instance of `LatentDirichletAllocation`. |

## Properties

| Property | Summary |
|:-----|:--------|
| `Components` | Gets the topic-word distribution matrix (topics x vocabulary). |
| `DocTopicPrior` | Gets the document-topic prior (alpha). |
| `LearningMethod` | Gets the learning method. |
| `NComponents` | Gets the number of topics. |
| `NVocab` | Gets the vocabulary size. |
| `SupportsInverseTransform` | Gets whether this transformer supports inverse transformation. |
| `TopicWordPrior` | Gets the topic-word prior (beta/eta). |

## Methods

| Method | Summary |
|:-----|:--------|
| `FitCore(Matrix<>)` | Fits LDA using variational inference. |
| `GetFeatureNamesOut(String[])` | Gets the output feature names after transformation. |
| `GetTopWordsPerTopic(Int32,String[])` | Gets the top words for each topic. |
| `InverseTransformCore(Matrix<>)` | Inverse transformation is not supported. |
| `TransformCore(Matrix<>)` | Transforms documents to topic distributions. |

