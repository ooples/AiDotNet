---
title: "SparseCodingAlgorithm"
description: "Specifies the algorithm for sparse coding (transform)."
section: "API Reference"
---

`Enums` · `AiDotNet.Preprocessing.DimensionalityReduction`

Specifies the algorithm for sparse coding (transform).

## Fields

| Field | Summary |
|:-----|:--------|
| `LASSO` | LASSO (L1 regularization) using coordinate descent. |
| `OMP` | Orthogonal Matching Pursuit - greedy selection. |

