---
title: "LossTask"
description: "Specific tasks a loss function is designed or commonly used for."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Specific tasks a loss function is designed or commonly used for.

## Fields

| Field | Summary |
|:-----|:--------|
| `AnomalyDetection` | Anomaly/outlier detection. |
| `BinaryClassification` | Binary classification (two classes). |
| `Denoising` | Denoising (noise removal). |
| `DepthEstimation` | Depth estimation. |
| `Embedding` | Embedding/metric learning. |
| `ImageGeneration` | Image generation/reconstruction. |
| `InstanceSegmentation` | Instance segmentation (per-object masks). |
| `MultiClass` | Multi-class classification (mutually exclusive classes). |
| `MultiLabel` | Multi-label classification (non-exclusive labels). |
| `ObjectDetection` | Object detection (bounding box + class). |
| `Ranking` | Ranking/retrieval tasks. |
| `Regression` | Continuous value regression. |
| `SemanticSegmentation` | Semantic segmentation (per-pixel classification). |
| `SuperResolution` | Super-resolution (upscaling). |
| `SurvivalAnalysis` | Survival analysis. |
| `TextGeneration` | Text generation/language modeling. |
| `TimeSeriesForecasting` | Time series forecasting. |

