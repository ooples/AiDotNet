---
title: "ModelCategory"
description: "Defines the algorithm family or category that a machine learning model belongs to."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Defines the algorithm family or category that a machine learning model belongs to.

## For Beginners

This tells you what kind of algorithm a model uses under the hood.
For example, a neural network learns by adjusting many interconnected weights, while
a decision tree learns by splitting data into branches based on feature values.
Understanding the category helps you choose the right tool for your problem.

## Fields

| Field | Summary |
|:-----|:--------|
| `Agent` | An autonomous agent that uses AI models to take actions and make decisions. |
| `AnomalyDetection` | A model designed specifically for detecting anomalies, outliers, or unusual patterns. |
| `Autoencoder` | An autoencoder that learns compressed representations by encoding and decoding data. |
| `Bayesian` | A Bayesian model that incorporates prior knowledge and provides posterior distributions. |
| `CausalModel` | A causal inference model that estimates cause-and-effect relationships. |
| `Classifier` | A classification model that assigns inputs to discrete categories. |
| `Clustering` | A clustering model that groups similar data points together without labels. |
| `ConvolutionalNetwork` | A convolutional neural network that learns spatial patterns using learnable filters. |
| `DecisionTree` | A decision tree model that makes predictions by learning hierarchical decision rules. |
| `Diffusion` | A diffusion model that generates data by learning to reverse a noise-adding process. |
| `EmbeddingModel` | A model that produces dense vector representations (embeddings) of inputs. |
| `Ensemble` | An ensemble model that combines multiple base models for better predictions. |
| `FoundationModel` | A large-scale foundation model pretrained on massive datasets. |
| `GAN` | A Generative Adversarial Network that learns by training a generator against a discriminator. |
| `GaussianProcess` | A Gaussian process model that provides probabilistic predictions with uncertainty estimates. |
| `GraphNetwork` | A graph neural network that operates on graph-structured data. |
| `InstanceBased` | An instance-based (lazy) learning method that stores training data for prediction. |
| `Interpretable` | A model designed for interpretability and explainability. |
| `Kernel` | A kernel-based method that maps data into higher-dimensional spaces. |
| `Linear` | A linear model that learns a linear relationship between features and targets. |
| `MetaLearning` | A meta-learning model that learns how to learn from few examples. |
| `NeuralNetwork` | A feedforward or general-purpose neural network. |
| `NeuralOperator` | A neural operator that learns mappings between function spaces. |
| `Optimization` | A model that uses optimization algorithms to find optimal parameters. |
| `PhysicsInformed` | A physics-informed model that incorporates physical laws as constraints. |
| `RecurrentNetwork` | A recurrent neural network that processes sequential data by maintaining hidden state. |
| `Regression` | A regression model that predicts continuous numeric values. |
| `Regularization` | A model with built-in regularization to prevent overfitting. |
| `ReinforcementLearningAgent` | A reinforcement learning agent that learns through trial-and-error interaction with an environment. |
| `SVM` | A support vector machine that finds optimal separating hyperplanes. |
| `SignalProcessing` | A classical signal processing method that uses mathematical transforms and filters. |
| `Statistical` | A statistical model based on classical statistical theory and distributions. |
| `SurvivalModel` | A survival analysis model that predicts time-to-event outcomes. |
| `SyntheticDataGenerator` | A model that generates synthetic data mimicking real dataset properties. |
| `TabularModel` | A model specifically optimized for structured tabular data. |
| `TimeSeriesModel` | A time series model specialized for sequential, time-dependent data. |
| `Transformer` | A transformer model based on self-attention mechanisms. |

