---
title: "SSLMethodCategory"
description: "Categorizes self-supervised learning methods by their learning paradigm."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Categorizes self-supervised learning methods by their learning paradigm.

## For Beginners

SSL methods can be grouped by how they learn representations:

## Fields

| Field | Summary |
|:-----|:--------|
| `Contrastive` | Contrastive learning methods (SimCLR, MoCo). |
| `Generative` | Generative self-supervised methods (MAE). |
| `NonContrastive` | Non-contrastive learning methods (BYOL, SimSiam, Barlow Twins). |
| `SelfDistillation` | Self-distillation methods (DINO, iBOT). |

