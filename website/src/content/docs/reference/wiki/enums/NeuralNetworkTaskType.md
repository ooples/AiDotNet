---
title: "NeuralNetworkTaskType"
description: "Defines the different types of tasks that a neural network can be designed to perform."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Defines the different types of tasks that a neural network can be designed to perform.

## For Beginners

Neural networks can solve many different types of problems. This enum lists 
the common problem types that neural networks can tackle. Think of it as a menu of what 
your AI can do - from sorting things into categories (classification), to predicting numbers 
(regression), to understanding images or text. Choosing the right task type helps the library 
set up the appropriate neural network structure for your specific problem.

## Fields

| Field | Summary |
|:-----|:--------|
| `AnomalyDetection` | Anomaly detection task |
| `AudioProcessing` | Audio processing task |
| `BinaryClassification` | Binary classification task (two classes) |
| `Clustering` | Clustering task |
| `Custom` | Custom task type |
| `DimensionalityReduction` | Dimensionality reduction task |
| `Generative` | Generative task (e.g., GANs, VAEs, image synthesis) |
| `ImageClassification` | Image classification task |
| `ImageSegmentation` | Image segmentation task |
| `MultiClassClassification` | Multi-class classification task (more than two classes) |
| `MultiLabelClassification` | Multi-label classification task (multiple labels can be assigned) |
| `NaturalLanguageProcessing` | Natural language processing task |
| `ObjectDetection` | Object detection task |
| `Recommendation` | Recommendation system task |
| `Regression` | Regression task (predicting continuous values) |
| `ReinforcementLearning` | Reinforcement learning task |
| `SequenceClassification` | Sequence-to-sequence classification |
| `SequenceToSequence` | Sequence-to-sequence task (e.g., machine translation) |
| `SpeechRecognition` | Speech recognition task |
| `TextGeneration` | Text generation task |
| `TimeSeriesForecasting` | Time series forecasting task |
| `TokenClassification` | Token classification task (per-position labeling) |
| `Translation` | Language translation |

