---
title: "ClusteringMetricType"
description: "Defines the types of metrics used to evaluate clustering quality."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Defines the types of metrics used to evaluate clustering quality.

## Fields

| Field | Summary |
|:-----|:--------|
| `AdjustedRandIndex` | Adjusted Rand Index (-1 to 1, higher is better). |
| `CalinskiHarabaszIndex` | Calinski-Harabasz Index (higher is better). |
| `DaviesBouldinIndex` | Davies-Bouldin Index (lower is better). |
| `NormalizedMutualInformation` | Normalized Mutual Information (0 to 1, higher is better). |
| `SilhouetteScore` | Silhouette Score (-1 to 1, higher is better). |

