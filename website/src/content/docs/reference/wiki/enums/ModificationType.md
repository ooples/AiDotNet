---
title: "ModificationType"
description: "Represents the types of modifications that can be applied to a model structure."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Represents the types of modifications that can be applied to a model structure.

## For Beginners

This enum represents different ways to change the structure of an AI model.
Think of a model as a network of connected nodes (similar to a flowchart). These values
describe the basic operations you can perform on this network: adding new nodes,
removing existing ones, or changing what a node does.

## Fields

| Field | Summary |
|:-----|:--------|
| `AddNode` | Adds a new node to the model structure. |
| `ChangeNodeType` | Changes the type or function of an existing node. |
| `RemoveNode` | Removes an existing node from the model structure. |

