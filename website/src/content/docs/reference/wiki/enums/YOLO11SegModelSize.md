---
title: "YOLO11SegModelSize"
description: "Defines the model size variants for YOLO11-Seg instance segmentation."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Defines the model size variants for YOLO11-Seg instance segmentation.

## For Beginners

YOLO11-Seg offers 25% latency reduction and improved small object detection
compared to YOLOv8-Seg while maintaining similar accuracy.

## How It Works

**Reference:** Ultralytics YOLO11, 2024.

## Fields

| Field | Summary |
|:-----|:--------|
| `L` | Large variant (~43.7M params). |
| `M` | Medium variant (~20.1M params). |
| `N` | Nano variant (~2.6M params). |
| `S` | Small variant (~9.6M params). |
| `X` | Extra-large variant (~62.1M params). |

