---
title: "NeuralArchitectureSearchStrategy"
description: "Specifies the strategy used for neural architecture search."
section: "API Reference"
---

`Enums` · `AiDotNet.Enums`

Specifies the strategy used for neural architecture search.

## Fields

| Field | Summary |
|:-----|:--------|
| `BayesianOptimization` | Uses Bayesian optimization to search for optimal architectures. |
| `Evolutionary` | Uses evolutionary algorithms to search for optimal architectures. |
| `GradientBased` | Uses gradient-based optimization (DARTS) to search for optimal architectures. |
| `RandomSearch` | Uses random search as a baseline. |
| `ReinforcementLearning` | Uses reinforcement learning to search for optimal architectures. |

