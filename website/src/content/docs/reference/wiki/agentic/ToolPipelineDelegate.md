---
title: "ToolPipelineDelegate"
description: "The delegate that invokes the next stage of a tool-invocation pipeline (ultimately the tool itself)."
section: "API Reference"
---

`Delegates` · `AiDotNet.Agentic.Pipeline`

The delegate that invokes the next stage of a tool-invocation pipeline (ultimately the tool itself).

