---
title: "ChatPipelineDelegate"
description: "The delegate that invokes the next stage of a chat-middleware pipeline (ultimately the model call)."
section: "API Reference"
---

`Delegates` · `AiDotNet.Agentic.Pipeline`

The delegate that invokes the next stage of a chat-middleware pipeline (ultimately the model call).

