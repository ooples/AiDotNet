---
title: "FederatedLearningComponentBase<T>"
description: "Base class for federated learning components that need numeric operations for a generic numeric type."
section: "API Reference"
---

`Base Classes` · `AiDotNet.FederatedLearning.Infrastructure`

Base class for federated learning components that need numeric operations for a generic numeric type.

## For Beginners

for provides AI safety functionality. Default values follow the original paper settings.

## Properties

| Property | Summary |
|:-----|:--------|
| `Engine` | Hardware-accelerated computation engine (CPU SIMD / GPU). |

