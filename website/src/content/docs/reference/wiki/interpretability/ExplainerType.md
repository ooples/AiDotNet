---
title: "ExplainerType"
description: "Explainer types available."
section: "API Reference"
---

`Enums` · `AiDotNet.Interpretability`

Explainer types available.

## Fields

| Field | Summary |
|:-----|:--------|
| `DeepSHAP` | DeepSHAP for neural networks. |
| `IntegratedGradients` | Integrated Gradients for neural networks. |
| `KernelSHAP` | Kernel SHAP (same as SHAP). |
| `LIME` | LIME (model-agnostic). |
| `SHAP` | Kernel SHAP (model-agnostic). |
| `TreeSHAP` | TreeSHAP for tree models. |

