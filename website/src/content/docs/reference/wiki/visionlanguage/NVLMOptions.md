---
title: "NVLMOptions"
description: "Options for NVLM 1.0 (cross-attention + decoder-only hybrid that retains text performance)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.VisionLanguage.InstructionTuned`

Options for NVLM 1.0 (cross-attention + decoder-only hybrid that retains text performance).

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `NVLMOptions(NVLMOptions)` | Initializes a new instance by copying from another instance. |

## Properties

| Property | Summary |
|:-----|:--------|
| `CrossAttentionDim` | Gets or sets the cross-attention dimension. |
| `EnableCrossAttention` | Gets or sets whether cross-attention hybrid mode is enabled. |

