---
title: "ModelPrecision"
description: "Specifies the precision mode for model inference and training."
section: "API Reference"
---

`Enums` · `AiDotNet.VisionLanguage.Encoders`

Specifies the precision mode for model inference and training.

## Fields

| Field | Summary |
|:-----|:--------|
| `BFloat16` | Brain floating point 16 (faster on modern GPUs). |
| `Float16` | 16-bit floating point (faster, lower memory). |
| `Float32` | 32-bit floating point (default, highest precision). |

