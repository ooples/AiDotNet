---
title: "TextEncoderVariant"
description: "Specifies the text encoder architecture."
section: "API Reference"
---

`Enums` · `AiDotNet.VisionLanguage.Encoders`

Specifies the text encoder architecture.

## Fields

| Field | Summary |
|:-----|:--------|
| `BERT` | BERT-based bidirectional encoder. |
| `LLMEnhanced` | LLM-enhanced text encoder (used by LLM2CLIP). |
| `MT5` | mT5-based multilingual text encoder (used by PaLI). |
| `RoBERTa` | RoBERTa-based bidirectional encoder (used by some variants). |
| `Transformer` | GPT-2-style transformer with causal attention (used by CLIP). |

