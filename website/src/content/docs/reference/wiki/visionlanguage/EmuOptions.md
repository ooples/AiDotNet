---
title: "EmuOptions"
description: "Options for Emu (unified VQA, captioning, generation via EVA-CLIP + LLM + diffusion)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.VisionLanguage.Generative`

Options for Emu (unified VQA, captioning, generation via EVA-CLIP + LLM + diffusion).

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `EmuOptions(EmuOptions)` | Initializes a new instance by copying from another instance. |

## Properties

| Property | Summary |
|:-----|:--------|
| `NumRegressionLayers` | Gets or sets the number of regression head layers. |
| `RegressionDim` | Gets or sets the visual regression head dimension (for image generation). |

