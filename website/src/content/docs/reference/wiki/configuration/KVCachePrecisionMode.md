---
title: "KVCachePrecisionMode"
description: "Controls the numeric precision of KV-cache storage."
section: "API Reference"
---

`Enums` · `AiDotNet.Configuration`

Controls the numeric precision of KV-cache storage.

## Fields

| Field | Summary |
|:-----|:--------|
| `Auto` | Select an industry-standard default. |
| `Float16` | Store KV-cache in FP16 (half precision) to reduce memory use. |
| `Float32` | Store KV-cache in FP32 (single precision) for maximal numerical fidelity. |

