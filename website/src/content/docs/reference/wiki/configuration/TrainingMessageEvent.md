---
title: "TrainingMessageEvent"
description: "Free-form text event for callers that just want to emit a typed log line at a specific level."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Configuration`

Free-form text event for callers that just want to emit a typed
log line at a specific level. Prefer the structured events above
when the data is repeating-shape-friendly.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `TrainingMessageEvent(TrainingDiagnosticLevel,String)` | Free-form text event for callers that just want to emit a typed log line at a specific level. |

