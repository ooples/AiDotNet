---
title: "YamlTimeSeriesModelSection"
description: "YAML section for selecting a time series model type by name."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Configuration`

YAML section for selecting a time series model type by name.

## For Beginners

Use this to specify which time series model to use in your YAML config.
The type should match one of the `TimeSeriesModelType` enum values (case-insensitive).

## How It Works

**Example YAML:**

## Properties

| Property | Summary |
|:-----|:--------|
| `Type` | The time series model type name. |

