---
title: "WaveGradOptions"
description: "Options for WaveGrad (gradient-based conditional waveform diffusion)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.TextToSpeech.Vocoders`

Options for WaveGrad (gradient-based conditional waveform diffusion).

## For Beginners

These options configure the WaveGrad model. Default values follow the original paper settings.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `WaveGradOptions(WaveGradOptions)` | Initializes a new instance by copying from another instance. |

