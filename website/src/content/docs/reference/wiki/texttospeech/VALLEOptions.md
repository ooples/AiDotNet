---
title: "VALLEOptions"
description: "Options for VALL-E (neural codec language model with AR + NAR transformers for zero-shot TTS)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.TextToSpeech.CodecBased`

Options for VALL-E (neural codec language model with AR + NAR transformers for zero-shot TTS).

## For Beginners

These options configure the VALLE model. Default values follow the original paper settings.

