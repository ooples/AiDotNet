---
title: "FreGradOptions"
description: "Options for FreGrad (frequency-domain diffusion vocoder with DWT sub-band processing)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.TextToSpeech.Vocoders`

Options for FreGrad (frequency-domain diffusion vocoder with DWT sub-band processing).

## For Beginners

These options configure the FreGrad model. Default values follow the original paper settings.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `FreGradOptions(FreGradOptions)` | Initializes a new instance by copying from another instance. |

