---
title: "IndexTTS<T>"
description: "IndexTTS: LLM-based zero-shot TTS with reference audio indexing for voice cloning."
section: "API Reference"
---

`Models & Types` · `AiDotNet.TextToSpeech.CodecBased`

IndexTTS: LLM-based zero-shot TTS with reference audio indexing for voice cloning.

## For Beginners

IndexTTS: LLM-based zero-shot TTS with reference audio indexing for voice cloning.. This model converts text input into speech audio output.

## How It Works

**References:**

- Project: "IndexTTS: An Industrial-Level Zero-Shot TTS System" (Index, 2024)

## Methods

| Method | Summary |
|:-----|:--------|
| `DecodeFromTokens(Tensor<>)` | Decodes codec tokens to audio. |
| `EncodeToTokens(Tensor<>)` | Encodes audio to codec tokens. |
| `Synthesize(String)` | Synthesizes speech. |

