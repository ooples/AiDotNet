---
title: "PriorGradOptions"
description: "Options for PriorGrad (diffusion vocoder with data-dependent prior for adaptive noise)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.TextToSpeech.Vocoders`

Options for PriorGrad (diffusion vocoder with data-dependent prior for adaptive noise).

## For Beginners

These options configure the PriorGrad model. Default values follow the original paper settings.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `PriorGradOptions(PriorGradOptions)` | Initializes a new instance by copying from another instance. |

