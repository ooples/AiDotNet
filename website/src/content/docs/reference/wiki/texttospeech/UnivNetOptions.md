---
title: "UnivNetOptions"
description: "Options for UnivNet (universal neural vocoder with multi-resolution spectrogram discriminator)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.TextToSpeech.Vocoders`

Options for UnivNet (universal neural vocoder with multi-resolution spectrogram discriminator).

## For Beginners

These options configure the UnivNet model. Default values follow the original paper settings.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `UnivNetOptions(UnivNetOptions)` | Initializes a new instance by copying from another instance. |

