---
title: "WaveRNNOptions"
description: "Options for WaveRNN (efficient autoregressive vocoder with subscale generation)."
section: "API Reference"
---

`Options & Configuration` · `AiDotNet.TextToSpeech.Vocoders`

Options for WaveRNN (efficient autoregressive vocoder with subscale generation).

## For Beginners

These options configure the WaveRNN model. Default values follow the original paper settings.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `WaveRNNOptions(WaveRNNOptions)` | Initializes a new instance by copying from another instance. |

