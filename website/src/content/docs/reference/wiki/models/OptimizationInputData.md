---
title: "OptimizationInputData<T, TInput, TOutput>"
description: "Represents the input data for optimization processes, including training, validation, and test datasets."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Models.Inputs`

Represents the input data for optimization processes, including training, validation, and test datasets.

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `OptimizationInputData` | Initializes a new instance of the OptimizationInputData class with empty datasets. |

## Properties

| Property | Summary |
|:-----|:--------|
| `InitialSolution` | Gets or sets the initial model/solution before optimization. |
| `XTest` | Gets or sets the input features for the test dataset. |
| `XTrain` | Gets or sets the input features for the training dataset. |
| `XValidation` | Gets or sets the input features for the validation dataset. |
| `YTest` | Gets or sets the target values for the test dataset. |
| `YTrain` | Gets or sets the target values for the training dataset. |
| `YValidation` | Gets or sets the target values for the validation dataset. |

