---
title: "GAMLSSDistributionFamily"
description: "Distribution families supported by GAMLSS."
section: "API Reference"
---

`Enums` · `AiDotNet.Models.Options`

Distribution families supported by GAMLSS.

## Fields

| Field | Summary |
|:-----|:--------|
| `Gamma` | Gamma distribution with shape (α) and rate (β) parameters. |
| `Laplace` | Laplace distribution with location (μ) and scale (b) parameters. |
| `LogNormal` | Log-Normal distribution with location (μ) and scale (σ) parameters. |
| `NegativeBinomial` | Negative Binomial distribution with mean and dispersion parameters. |
| `Normal` | Normal (Gaussian) distribution with location (μ) and scale (σ) parameters. |
| `Poisson` | Poisson distribution with rate (λ) parameter. |
| `StudentT` | Student-t distribution with location (μ), scale (σ), and degrees of freedom (ν). |

