---
title: "CovarianceType"
description: "Type of covariance parameters to estimate."
section: "API Reference"
---

`Enums` · `AiDotNet.Clustering.Options`

Type of covariance parameters to estimate.

## Fields

| Field | Summary |
|:-----|:--------|
| `Diagonal` | Each component has its own diagonal covariance matrix. |
| `Full` | Each component has its own general covariance matrix. |
| `Spherical` | Each component has its own single variance. |
| `Tied` | All components share the same general covariance matrix. |

