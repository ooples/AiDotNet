---
title: "ImageTensorLayout"
description: "Specifies the axis ordering for image tensors returned by vision data loaders."
section: "API Reference"
---

`Enums` · `AiDotNet.Data.Vision.Benchmarks`

Specifies the axis ordering for image tensors returned by vision data loaders.

## Fields

| Field | Summary |
|:-----|:--------|
| `NCHW` | Channel-first layout: `[B, C, H, W]`. |
| `NHWC` | Channel-last layout: `[B, H, W, C]`. |

