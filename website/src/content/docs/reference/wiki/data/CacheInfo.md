---
title: "CacheInfo"
description: "Information about the current state of a pipeline cache."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Data.Pipeline`

Information about the current state of a pipeline cache.

## Properties

| Property | Summary |
|:-----|:--------|
| `CacheDirectory` | Path to the cache directory. |
| `EntryCount` | Number of cached entries (tensors). |
| `FormattedSize` | Human-readable cache size. |
| `IsValid` | Whether the cache is valid and usable. |
| `TotalSizeBytes` | Total cache size in bytes. |

