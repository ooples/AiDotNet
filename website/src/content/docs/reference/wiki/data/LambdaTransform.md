---
title: "LambdaTransform<TInput, TOutput>"
description: "Wraps a `Func` delegate as an `ITransform`."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Data.Transforms`

Wraps a `Func` delegate as an `ITransform`.

## For Beginners

Use this when you need a quick, inline transform
without creating a full class:

## Constructors

| Constructor | Summary |
|:-----|:--------|
| `LambdaTransform(Func<,>)` | Creates a new lambda transform from the given function. |

## Methods

| Method | Summary |
|:-----|:--------|
| `Apply()` |  |

