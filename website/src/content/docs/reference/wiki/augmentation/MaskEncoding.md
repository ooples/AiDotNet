---
title: "MaskEncoding"
description: "Specifies the encoding format for segmentation masks."
section: "API Reference"
---

`Enums` · `AiDotNet.Augmentation.Image`

Specifies the encoding format for segmentation masks.

## Fields

| Field | Summary |
|:-----|:--------|
| `Dense` | Raw 2D array of pixel values. |
| `Polygon` | Polygon vertices. |
| `RLE` | Run-length encoding (COCO-style). |

