---
title: "PaddingMode"
description: "Specifies the padding mode for filling new pixels."
section: "API Reference"
---

`Enums` · `AiDotNet.Augmentation.Image`

Specifies the padding mode for filling new pixels.

## Fields

| Field | Summary |
|:-----|:--------|
| `Constant` | Fill with a constant value (default 0). |
| `Edge` | Replicate edge pixels outward. |
| `Reflect` | Reflect pixels at the boundary (not duplicating the edge pixel). |
| `Symmetric` | Reflect pixels at the boundary (duplicating the edge pixel). |

