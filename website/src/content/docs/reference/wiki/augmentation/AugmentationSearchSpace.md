---
title: "AugmentationSearchSpace"
description: "Represents the search space for augmentation hyperparameters."
section: "API Reference"
---

`Models & Types` · `AiDotNet.Augmentation`

Represents the search space for augmentation hyperparameters.

## Properties

| Property | Summary |
|:-----|:--------|
| `AugmentationType` | Gets or sets the augmentation type name. |
| `Hyperparameters` | Gets or sets the hyperparameter definitions. |
| `IncludeInSearch` | Gets or sets whether to include this augmentation in the search. |
| `IncompatibleWith` | Gets or sets incompatible augmentations (cannot be used together). |
| `ProbabilityRange` | Gets or sets the probability search range. |
| `RequiredWith` | Gets or sets required augmentations (must be used together). |

