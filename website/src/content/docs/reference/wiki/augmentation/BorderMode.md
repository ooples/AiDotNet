---
title: "BorderMode"
description: "Specifies how to handle pixels that fall outside the image bounds during transformation."
section: "API Reference"
---

`Enums` · `AiDotNet.Augmentation.Image`

Specifies how to handle pixels that fall outside the image bounds during transformation.

## Fields

| Field | Summary |
|:-----|:--------|
| `Clamp` | Clamp to the nearest edge pixel. |
| `Constant` | Fill with a constant value. |
| `Reflect` | Reflect pixels at the edge (mirror). |
| `Wrap` | Wrap around to the opposite edge. |

