---
title: "ProgramSynthesisModelKind"
description: "Defines the built-in program synthesis model implementations that can be configured via the primary builder/result APIs."
section: "API Reference"
---

`Enums` · `AiDotNet.ProgramSynthesis.Options`

Defines the built-in program synthesis model implementations that can be configured via the primary builder/result APIs.

## Fields

| Field | Summary |
|:-----|:--------|
| `CodeBERT` | CodeBERT (encoder-only) for code understanding tasks. |
| `CodeT5` | CodeT5 (encoder-decoder) for code generation and transformation tasks. |
| `GraphCodeBERT` | GraphCodeBERT (encoder-only) with optional data-flow modeling. |

