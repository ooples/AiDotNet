---
title: "ICodeTokenizationPipeline"
description: "Defines a code tokenization pipeline that builds on the core tokenizer stack and adds code-oriented metadata."
section: "API Reference"
---

`Interfaces` · `AiDotNet.ProgramSynthesis.Tokenization`

Defines a code tokenization pipeline that builds on the core tokenizer stack and adds code-oriented metadata.

