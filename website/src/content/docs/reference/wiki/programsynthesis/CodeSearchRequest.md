---
title: "CodeSearchRequest"
description: "Request for searching code using a query against a corpus."
section: "API Reference"
---

`Models & Types` · `AiDotNet.ProgramSynthesis.Requests`

Request for searching code using a query against a corpus.

## Properties

| Property | Summary |
|:-----|:--------|
| `Corpus` | The search corpus definition (request-scoped docs and/or an indexed corpus reference). |
| `Filters` | Optional filters expressed as simple strings (Serving may interpret/enforce these). |

