<Project Sdk="Microsoft.NET.Sdk">
  <!-- TEMPORARY DIAGNOSTIC SCAFFOLDING — deleted before the fix PR is opened.
       Deliberately NOT added to AiDotNet.sln, so no other workflow's root
       restore/build can pick it up. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <RootNamespace>AiDotNet.Tools.StyDiffCloneProbe</RootNamespace>
    <AssemblyName>StyDiffCloneProbe</AssemblyName>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="Program.cs" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\AiDotNet.csproj" />
  </ItemGroup>
</Project>
